authentik:
  error_reporting:
    enabled: true

ingress:
  enabled: true
  annotations:
    external-dns.alpha.kubernetes.io/target: auth.mikenabhan.com
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
    cert-manager.io/cluster-issuer: prod
  hosts:
    - host: auth.mikenabhan.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
      - auth.mikenabhan.com
      secretName: authentik

postgresql:
  enabled: true
redis:
  enabled: true